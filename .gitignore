node_modules
dist
.DS_Store
*.local

# collected data and its aggregates are produced by scripts/, not committed by hand
# (the collect workflow commits data/raw itself)
src/data/measured/*
!src/data/measured/network-times.json

# collected OSM places are open data (ODbL) and are committed
!src/data/measured/places.json

# secrets
.env
.env.local


# spend ledger, local only
data/.budget/
